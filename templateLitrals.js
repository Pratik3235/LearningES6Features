// Template literals were introduced in ES6 (ES2015)
// Use backticks (` `) instead of quotes for strings
// Support string interpolation using ${expression}

let val = "Pratik";
function greet(name) {
    console.log(`Hello ${name}`);
}
greet(val);